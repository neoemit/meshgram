services:
  meshgram:
    build: .
    container_name: meshgram
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MESHGRAM_CONFIG_PATH: /app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
